# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
/test-results/

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*

# deployment and generated files
.vercel
next-env.d.ts
*.tsbuildinfo
/dist/
/.wrangler/
/.wrangler-home/
/outputs/
/output/
/work/
/.playwright-cli/
/tmp/
/playwright-report/
/test-results/

# forensic audit output (regenerate with playwright.audit.config.ts)
/audit-out/

# deploy lock capability token — proves this worktree holds the lock
.deploy-lock-token
.indexnow-state.json

# Built by `npm run portable`. A ~200 MiB copy of dist/client; the recipe is in
# scripts/build-portable.mjs, the artifact belongs on a USB stick, not in git.
/release/opentools-portable/
